{% extends 'base.html.twig' %}

{% block body %}
    <h1>Presse creation</h1>

    {{ form_start(form) }}
    {% for presse in langue %}
        {{ form_widget(form) }}
    {% endfor %}
    <input type="submit" value="Create" />
    {{ form_end(form) }}
    <ul>
        <li>
            <a href="{{ path('presse_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
