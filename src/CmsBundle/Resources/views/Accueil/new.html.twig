{% extends 'base.html.twig' %}

{% block body %}
    <h1>Accueil creation</h1>

    {{ form_start(form) }}
    {% for accueil in langue %}
        {{ form_widget(form) }}
    {% endfor %}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('accueil_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
