{% extends 'base.html.twig' %}

{% block body %}
    <h1>Apropos creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('user_apropos') }}">Retour à la page à propos</a>
        </li>
    </ul>
{% endblock %}
